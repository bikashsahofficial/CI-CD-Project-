name: say hello

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello World!"

      - name: Check date
        run: date

      - name: List files
        run: ls -l